export enum Metrics {
    ThrottleLocation = 'throttle-location',
    ThrottleGlobal = 'throttle-global',
    ThrottleTestPush = 'throttle-test-push',
    GraphQLError = 'graphql-error',
    QueryLocation = 'query-location',
}
