# this allows us to define variables in debug mode only

dev:
  STAGE: ${self:custom.stage}
  dataservice_arn:
    Fn::ImportValue: DataArn-${self:custom.stage}
  allowed_countries: de

test:
  STAGE: ${self:custom.stage}
  dataservice_arn:
    Fn::ImportValue: DataArn-${self:custom.stage}
  allowed_countries: de

prod:
  STAGE: ${self:custom.stage}
  dataservice_arn:
    Fn::ImportValue: DataArn-${self:custom.stage}
  allowed_countries: ag,au,be,ca,cy,dk,ee,de,gi,gb,in,il,it,mg,mw,mu,ma,np,nl,no,ph,ro,ru,sc,sg,za,lk,fi,se,tt,sr,tn,us,zm,zw,mc,lu,hu,bg,is,sn,pt,ch,sm,nz,md,ea,mt,my,lt,lv,hk,bw,bd,at,fr
  maintenance: false
