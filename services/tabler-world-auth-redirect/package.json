{
  "name": "@mskg/tabler-world-auth-redirect",
  "author": "Markus Kling",
  "license": "Copyright 2019-present Markus Kling",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
    "serverless": "^1.49.0",
    "serverless-sync-s3buckets": "^1.0.1"
  },
  "scripts": {
    "deploy": "env-cmd --no-override ../../config/.env cross-env-shell \"serverless deploy --force --verbose\"",
    "pack": "env-cmd --no-override ../../config/.env cross-env-shell \"serverless package --verbose\"",
    "remove": "env-cmd --no-override ../../config/.env cross-env-shell \"serverless remove --verbose\"",
    "start": "ntl"
  }
}
