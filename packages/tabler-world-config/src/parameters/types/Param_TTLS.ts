export type Param_TTLS = {
    // Individual and all membes
    Member: number;
    MemberOverview: number;

    // Family, Area, ...
    Structure: number;
    StructureOverview: number;

    // API Cache
    Albums: number;
    Documents: number;
    News: number;

    // Authentication
    Principal: number;

    // I18N
    I18N: number;

    // Member properties
    ChatEnabled: number;
    LocationEnabled: number;

    // Conversations
    ChatConversations: number;
};
